<div class="welcome-container">
  <div class="welcome-card">
    <h2>View Site as Guest</h2>
    <div class="guest-card">
      <p>This site is a simple app used to visualize what packaging machines are running at any time. Future iterations include scheduling and logs.</p>
      <br>
      <%= link_to "Click to View", welcome_path, class:"welcome-button" %>
    </div>
  </div>
  <div class="welcome-card">
    <h2>Welcome to Great Divide Status</h2>
    <div class="login-card">
      <%= form_for :session, url: login_path do |f| %>
      <br>
      <%= f.label :email %>
      <%= f.email_field :email, class:'input-box', autofocus: true %>
      <br>
      <br>
      <%= f.label :password %>
      <%= f.password_field :password, class:'input-box' %>
      <br>
      <br>
      <%= f.submit 'Log In', class: "welcome-button" %>
      <% end %>
    </div>
    <%= link_to "Forgot Password?", welcome_path, class:"forgot-link" %>

  </div>

</div>
